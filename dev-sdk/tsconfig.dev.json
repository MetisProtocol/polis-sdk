{
  "extends": "./config/tsconfig.base.json",
  "compilerOptions": {                         /* Concatenate and emit output to single file. */
    "outDir": "dist",
  },
  "include": [
    "./lib"
  ],
  "exclude": ["node_modules", "./test"]
}
